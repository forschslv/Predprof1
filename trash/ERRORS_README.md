# Универсальная страница ошибок

## Обзор

Реализована универсальная страница ошибок с динамическим рендерингом через JavaScript. Система позволяет:
- Отображать различные типы ошибок (400, 401, 403, 404, 500, 503, сетевые ошибки)
- Передавать кастомные сообщения и детали ошибок
- Копировать детали ошибок в буфер обмена
- Интегрироваться с существующим API

## Файлы

1. **error.html** - Основная страница ошибок
2. **error_render.js** - JavaScript для динамического рендеринга
3. **test_error.html** - Тестовая страница для демонстрации
4. **script.js** - Обновлён с интеграцией обработки ошибок

## Использование

### 1. Прямой переход на страницу ошибок

```javascript
// Переход с параметрами через URL
window.location.href = 'error.html?code=404&message=Страница не найдена&details=Дополнительная информация';

// Или через функцию из error_render.js
window.showErrorPage('404', 'Страница не найдена', 'Дополнительная информация');
```

### 2. Интеграция с API запросами

Функция `apiRequest` в `script.js` автоматически перенаправляет на страницу ошибок при:
- Ошибке авторизации (401)
- Ошибке доступа (403)
- Серверных ошибках (500, 503)
- Сетевых ошибках

### 3. Обработка ошибок API

```javascript
// Использование встроенного обработчика
fetch('/api/endpoint')
    .then(response => {
        if (!response.ok) {
            return window.handleApiError(response);
        }
        return response.json();
    })
    .catch(error => {
        window.showErrorPage('network', 'Ошибка сети', error.message);
    });
```

### 4. Кастомные ошибки

```javascript
// Создание собственной ошибки
window.showErrorPage('418', 'Я - чайник', 'Попытка заварить кофе в чайнике');

// Или через URL параметры
const params = new URLSearchParams({
    code: '999',
    message: 'Кастомная ошибка',
    details: 'Любые детали здесь',
    timestamp: new Date().toISOString()
});
window.location.href = `error.html?${params.toString()}`;
```

## Конфигурация ошибок

Конфигурации хранятся в `error_render.js` в объекте `ERROR_CONFIGS`:

```javascript
const ERROR_CONFIGS = {
    '400': {
        title: 'Некорректный запрос',
        description: 'Сервер не может обработать ваш запрос...',
        icon: '❌',
        showDetails: true
    },
    // ... другие ошибки
};
```

## Тестирование

1. Откройте `test_error.html` для тестирования всех типов ошибок
2. Или используйте кнопку "🐛 Тест ошибок" в главном меню
3. Для разработчиков: проверьте консоль браузера для логов

## Особенности

### Локализация
- Все тексты на русском языке
- Время отображается в формате локали 'ru-RU'

### Адаптивность
- Страница полностью адаптивна
- Использует CSS переменные из `dashboard.css`

### Безопасность
- Детали ошибок скрыты по умолчанию
- Пользователь может показать/скрыть детали
- ID ошибки генерируется уникальный для каждой сессии

## Интеграция с существующим кодом

### Для новых страниц:
1. Подключите `error_render.js`
2. Используйте `window.showErrorPage()` для отображения ошибок

### Для существующих страниц:
1. Обновите обработку ошибок в API запросах
2. Замените `alert()` на вызовы функций из `error_render.js`

## Примеры использования

### Пример 1: Обработка ошибки загрузки данных
```javascript
async function loadUserData() {
    try {
        const data = await apiRequest('/users/me');
        // Обработка данных
    } catch (error) {
        // Автоматически перенаправит на страницу ошибок
        console.error('Failed to load user data:', error);
    }
}
```

### Пример 2: Кастомная валидация
```javascript
function validateForm(data) {
    if (!data.email) {
        window.showErrorPage('400', 'Не заполнено поле email', 'Поле email обязательно для заполнения');
        return false;
    }
    return true;
}
```

## Поддержка

При возникновении проблем:
1. Проверьте консоль браузера на наличие ошибок JavaScript
2. Убедитесь, что файлы `error.html` и `error_render.js` доступны
3. Проверьте, что в localStorage нет конфликтующих данных

## Лицензия

Часть проекта "Столовая". Свободно для использования в рамках проекта.